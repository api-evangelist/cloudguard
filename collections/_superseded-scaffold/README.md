# Superseded collections

These Postman / OpenCollection files were derived from the superseded best-effort scaffold
OpenAPI (see `openapi/_superseded-scaffold/`). They describe operations Check Point does not
publish under those identifiers. Retained for audit only; not referenced from `apis.yml`.
